// import { ReactElement } from "react"

interface CardProps{
    title: string
}

export const Card = (props: CardProps)=>{

    return <>
        <div>
            <div className="flex">
                <div></div>
            </div>

        </div>
    </>
}